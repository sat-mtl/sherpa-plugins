// Implementation is inline in Diarization.hpp (see OfflineRecognizer.cpp for why:
// several Avendish back-ends compile the header directly and do not link this
// library). This TU only gives the CMake target a source to compile.
#include "Diarization.hpp"
